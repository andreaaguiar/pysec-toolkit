# Web-based tool dependencies
requests~=2.32.4      # For HTTP requests in web scanning and directory/subdomain enumeration
beautifulsoup4~=4.14.2 # For HTML parsing in web vulnerability scanner

# Network and SSH tools
paramiko~=3.5.1       # For SSH connections in ssh-brute-force tool
scapy~=2.6.1          # For network scanning and ARP operations

# Utility libraries
tqdm~=4.67.1          # For progress bars in various scripts
