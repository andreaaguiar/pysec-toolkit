# Core dependencies for all scripts
requests>=2.32.3      # For HTTP requests in web scanning and directory/subdomain enumeration
urllib3>=2.4.0        # Used by requests but specified explicitly for version control
beautifulsoup4>=4.13.4 # For HTML parsing in web vulnerability scanner

# Network and SSH tools
paramiko>=3.5.1       # For SSH connections in ssh-brute-force tool
scapy>=2.6.1          # For network scanning and ARP operations

# Utility libraries
tqdm>=4.67.1          # For progress bars in various scripts

# Optional/specialized dependencies
dnspython>=2.7.0      # For DNS operations in subdomain enumeration
